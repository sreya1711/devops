                                           DAY 4
📌 Installation of JDK and Jenkins on Ubuntu
✅ Step 1: Update System Packages
sudo apt update
sudo apt upgrade -y
________________________________________
✅ Step 2: Install Java (JDK)
Jenkins requires Java (JDK 17 or later recommended).
sudo apt install openjdk-17-jdk -y
🔎 Verify Java Installation
java -version
You should see OpenJDK version details.
________________________________________
✅ Step 3: Add Jenkins Repository Key
curl -fsSL https://pkg.jenkins.io/debian-stable/jenkins.io-2023.key | sudo tee \
  /usr/share/keyrings/jenkins-keyring.asc > /dev/null
________________________________________
✅ Step 4: Add Jenkins Repository
echo deb [signed-by=/usr/share/keyrings/jenkins-keyring.asc] \
  https://pkg.jenkins.io/debian-stable binary/ | sudo tee \
  /etc/apt/sources.list.d/jenkins.list > /dev/null
________________________________________
✅ Step 5: Update Package List Again
sudo apt update
________________________________________
✅ Step 6: Install Jenkins
sudo apt install jenkins -y
________________________________________
✅ Step 7: Start Jenkins Service
sudo systemctl start jenkins
________________________________________
✅ Step 8: Enable Jenkins at Boot
sudo systemctl enable jenkins
________________________________________
✅ Step 9: Check Jenkins Status
sudo systemctl status jenkins
________________________________________
✅ Step 10: Access Jenkins in Browser
Open browser and type:
http://localhost:8080
If using VM/server:
http://your-server-ip:8080
________________________________________
✅ Step 11: Get Initial Admin Password
sudo cat /var/lib/jenkins/secrets/initialAdminPassword
Copy the password and paste it in Jenkins setup page.
PART 1: Creating a Freestyle Project in Jenkins
✅ Step 1: Open Jenkins Dashboard
Open browser:
http://localhost:8080
Login with admin credentials.
________________________________________
✅ Step 2: Create New Item
1.	Click New Item
2.	Enter project name → Freestyle-Demo
3.	Select Freestyle project
4.	Click OK
________________________________________
✅ Step 3: Configure Freestyle Project
🔹 General Section
✔️ You can add description (optional)
________________________________________
🔹 Build Triggers (To Auto Trigger Build)
You can choose:
•	✔️ Build periodically
o	Example (every minute):
•	* * * * *
•	✔️ Poll SCM
o	Example:
•	H/2 * * * *
________________________________________
🔹 Build Section
1.	Click Add build step
2.	Select Execute shell
3.	Add simple command:
echo "Hello Jenkins Freestyle Project"
________________________________________
✅ Step 4: Save Project
Click Save
________________________________________
✅ Step 5: Trigger the Build
🔹 Manual Trigger
Click Build Now
🔹 View Console Output
Click build number → Console Output
You should see:
Hello Jenkins Freestyle Project
Finished: SUCCESS
________________________________________
🔷 PART 2: Creating a Pipeline Project
Pipeline uses script (Jenkinsfile).
________________________________________
✅ Step 1: Create New Item
1.	Click New Item
2.	Enter name → Pipeline-Demo
3.	Select Pipeline
4.	Click OK
________________________________________
✅ Step 2: Configure Pipeline
Scroll to Pipeline section
Select:
•	Definition → Pipeline script
________________________________________
✅ Step 3: Add Pipeline Script
Paste this basic pipeline:
pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building the project...'
            }
        }

        stage('Test') {
            steps {
                echo 'Testing the project...'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying the project...'
            }
        }
    }
}
________________________________________
✅ Step 4: Save
Click Save
________________________________________
✅ Step 5: Trigger Pipeline
Click Build Now
Click build number → View:
•	Console Output
•	Stage View
You will see 3 stages:
•	Build
•	Test
•	Deploy
All should show SUCCESS ✅

